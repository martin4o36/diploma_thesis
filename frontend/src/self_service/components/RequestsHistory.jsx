import { useState } from "react";

function RequestsHistory({ holiday_requests, remote_requests }) {
    return(
        <div>
            Requests History
        </div>
    );
};

export default RequestsHistory;